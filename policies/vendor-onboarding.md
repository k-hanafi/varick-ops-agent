# Vendor Onboarding

New vendors are created with a status of `under_review` while compliance
verifies their tax, banking, and ownership details.

A vendor becomes `active` only after this verification completes. Only `active`
vendors are eligible to receive payments. A vendor may be returned to
`under_review` at any time if a compliance or fraud concern is raised.
